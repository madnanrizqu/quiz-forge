# Widgets Layer

**Why it exists:**
Contains large, self-contained chunks of functionality or UI that deliver a complete use case (e.g., a Header, a Quiz Player, an Article Feed).

**How to use it:**
Combine Features, Entities, and Shared components into complex, standalone blocks. Widgets can be reused across multiple pages but **cannot** import from other widgets.
